qlib_init:
    provider_uri: "~/.qlib/qlib_data/kr_data"
    region: us  # no built-in kr region; us defaults (trade_unit=1) fit KRX, rest overridden below
market: &market all
benchmark: &benchmark 069500  # KODEX 200 ETF
data_handler_config: &data_handler_config
    start_time: 2018-01-01
    end_time: 2026-06-30
    fit_start_time: 2018-01-01
    fit_end_time: 2023-12-31
    instruments: *market
port_analysis_config: &port_analysis_config
    strategy:
        class: TopkDropoutStrategy
        module_path: qlib.contrib.strategy
        kwargs:
            signal: <PRED>
            topk: 5
            n_drop: 1
    backtest:
        start_time: 2025-01-01
        end_time: 2026-06-30
        account: 100000000
        benchmark: *benchmark
        exchange_kwargs:
            limit_threshold: 0.3
            deal_price: close
            open_cost: 0.00015
            close_cost: 0.0017   # commission + KR securities transaction tax on sell
            min_cost: 0
            trade_unit: 1
task:
    model:
        class: LGBModel
        module_path: qlib.contrib.model.gbdt
        kwargs:
            loss: mse
            learning_rate: 0.1
            max_depth: 6
            num_leaves: 64
            num_threads: 8
    dataset:
        class: DatasetH
        module_path: qlib.data.dataset
        kwargs:
            handler:
                class: Alpha158
                module_path: qlib.contrib.data.handler
                kwargs: *data_handler_config
            segments:
                train: [2018-01-01, 2023-12-31]
                valid: [2024-01-01, 2024-12-31]
                test: [2025-01-01, 2026-06-30]
    record:
        - class: SignalRecord
          module_path: qlib.workflow.record_temp
          kwargs:
            model: <MODEL>
            dataset: <DATASET>
        - class: SigAnaRecord
          module_path: qlib.workflow.record_temp
          kwargs:
            ana_long_short: False
            ann_scaler: 252
        - class: PortAnaRecord
          module_path: qlib.workflow.record_temp
          kwargs:
            config: *port_analysis_config
